import { MouseParallaxDirective } from './mouseParallax.directive';

describe('ParallaxDirective', () => {
  it('should create an instance', () => {
    const directive = new MouseParallaxDirective();
    expect(directive).toBeTruthy();
  });
});
